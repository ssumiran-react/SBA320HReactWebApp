This is a simple React application that demonstrates client-side routing using 
- react-router-dom (BrowserRouter, Routes, Route and Link)
- Bootstrap
- CSS
- Javascript
 
The app contains two pages for different games(home) and about links.

The application consumes public game API and display the data.